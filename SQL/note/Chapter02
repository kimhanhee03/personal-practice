--Chapter 2장 > 관계 데이터 모델
/*
     테이블: 데이터를 저장한 단위(파일)
       ㄴ릴레이션
     구조: 이차원 형식
	    ROW/COLUMN
	
		COLUMN: 중복이 있으면 안된다/순서가 없다 > 자바-class 멤버변수
		ROW: > 자바-멤버변수의 초기화: 객체
		
		> 순서는 관계 없다
		
		테이블(릴레이션)
		:관련된 데이터를 한곳에 모아서 관리가쉽게 > List
		:테이블은 한개의 데이터베이스 안에서 유일 값이다 
		:클래스 > 멤버변수,테이블 > 컬럼
		                    |			 |	
				    ------------------
	     	:컬럼은 순서가 없다
		:데이터 순서가 없다 > 단점(번호가 섞이는 경우도 있다)
					       정렬: ORDER BY(사용하지 않는다) > 인덱스
