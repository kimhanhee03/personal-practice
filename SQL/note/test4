--오라클 2일차 > WHERE / ORDER BY
-- WHERE 조건 > 연산자 / 정확한 검색 > 내장 함수(단일 함수, 집계 함수)
--145page
--GROUP BY / JOIN / SubQuery > SELECT
--DDL(Table) > INSERT  / UPDATE / DELETE
--고급: View / Index / Swquence / PL/SQL
--데이터베이스 설계 (ER_MODEL) / 정규화
--트랜잭션 / 보안 / 복원 / 백업
/*
    오라클: 경우의 수가 많다(SQL문장 여러개가 존재)
     > 결과값 출력 / 최적화
    SQL:구조화된 질의 언어
		ㄴ문자형식으로 제작: 순서/형식
			 ㄴ여러개를 사용할 수 있다 > 본인 스타일을 만든다
       =DML: 데이터 조작언어
	         SELECT: 데이터 검색 (70%)
		 INSERT: 데이터 추가 (
		 UPDATE: 데이터 수정
		 DELETE: 데이터 삭제
       =DDL: 데이터 정의 언어 > TABLE(데이터 저장장소) > 파일
					    View(가상 테이블) > 보안 / 단순한 쿼리
					      ㄴSELECT ~
					    SWQUENCE: 자동증가(PRIMARY KEY)
					    INDEX: 검색, 정렬 > 속도의 최적화
					      ㄴPL/SQL: 호불호 > 재사용 > ERP
					         댓글
					    FUNCTION: 사용자 정의 함수
					    PROCEDURE: 사용자 정의 함수 > 기능
					    TRIGGER: 자동화 처리	
		  CREATE:생성
		  DROP:삭제
		  ALTER:수정
		  TRUNCATE:데이터만 잘라내기
		  RENAME:이름변경
	=DCL: 권한부여/해제
		 GRANT: 권한 부여
		 REVOKE: 권한 해제 > View / INDEX / 시노님 > 권한이 없다
	=TCL: 정상 저장 / 모든 명령 취소 > 일괄처리
								ㄴ트랜잭션	
		   COMMIT: 정상적으로 저장
		   ROLLBACK: 명령 취소
		   SAVEPOINT: 지정된 위치부터 취소
	
	1. SELECT: 데이터 검색
	    =형식 / 순서
		필수
		   SELECT * | column_list(원하는 컬럼만 선택)
		   FROM table_name | view_name | SELECT ~~ (인라인뷰) > 패이징

		[
			WHERE 조건
			GROUP BY 그룹컬럼
			HAVING 그룹에 대한 조건 > 반드시 GROUP BY 동반
			ORDER BY 컬럼 (ASC|DESC)
						 ㄴdefault(생략이 가능)
		]
	=처리 순서
		FROM - WHERE - GROUP BY - HAVING - SELECT - ORDER BY	

	 데이터 베이스: 폴더
	 테이블: 파일
	 컬럼: 멤버변수
	 ROW(Tuple): 인스턴스 > 객체

	 **컬럼으로 구분: 관리가 쉽다
	 **명령어 > 읽기/쓰기 편함
	 **조건 > 검색 > WHERE 컬럼명 연산자 값

	7.LIKE > SELECT문장 핵심은 검색 > 패턴
	   유사 문자열 검색
	     %: 문자의 갯수를 모르는 경우
	     _ : 문자 한글자
	   > contains > %검색어%
	   > startWith > 검색어%
	   > endWith > %검색어
	   > 5글자 가운데 C > _C_
	   > 3글자 A   A_
	   > REGEXP_LIKE() > 정규식
	   
	   사용자 대신 > SQL문장을 만들어서 처리  > 웹 개발자
    		
*/
/*
select ename
from emp
where ename like '%A%';

select ename
from emp
where ename like 'A%';

select ename
from emp
where ename like '%EN' OR ename like '%LN';

select ename
from emp
where ename like '__O__';

select ename
from emp
where ename like '__A%';

select *
from book
where bookname like '%축구%';

select *
from book
where bookname like '_구%';

select * from customer
where PHONE is null;

select * 
from customer
where address like '%대한민국%';

*/
--산술연산자
--select 10+5, 10-5, 10*5, 10/3
--from book;
--범위, 기간 > between ~ and	
